from django.apps import AppConfig


class ConsultaConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'consulta'
    verbose_name = 'Painel SEAMI de Consulta Externa (Read-Only)'
